#include "main.h"

/**
    *set_string - function tha sets the value of a pointer
    *to a char
    *@s: string character
    *@to: the valuer of pointer
    *Return: success
    */

void set_string(char **s, char *to)
{
		*s = to;
}
