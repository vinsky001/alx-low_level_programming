#include <string.h>
#include "main.h"

/**
    *_strcmp - this function compares two strings
    *@s1: first string argument character
    *@s2: second string argument character
    *Return: compared values
    */

int _strcmp(char *s1, char *s2)
{
	return (strcamp(s1, s2));
}
